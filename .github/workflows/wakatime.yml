name: Waka Readme

# Impede execuções duplicadas se você clicar no botão várias vezes rapidamente
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # Remove o aviso de depreciação do Node.js nos logs
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  schedule:
    # Roda diariamente às 18:30 UTC
    - cron: '30 18 * * *'
  workflow_dispatch:

jobs:
  update-readme:
    name: Update Readme with Metrics
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # Checkout usando seu PAT para garantir permissão de escrita (evita erro 403)
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.WAKATIME_TOKEN }}

      - name: Rodar WakaStats
        id: waka_job
        uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.WAKATIME_TOKEN }}
          SHOW_LINES_OF_CODE: "true"
          SHOW_SHORT_INFO: "false"
          SHOW_PROFILE_VIEWS: "false"
          SHOW_TOTAL_CODE_TIME: "false"
          COMMIT_MESSAGE: "Updated WakaTime stats [skip ci]"

      - name: Notificar via Resend
        # Só dispara se o workflow foi manual ou agendado, e se o passo anterior deu certo
        if: success() && steps.waka_job.outcome == 'success' && github.event_name != 'push'
        env:
          RESEND_KEY: ${{ secrets.RESEND_API_KEY }}
        run: |
          NOW=$(date +'%d/%m/%Y %H:%M')
          curl -s -X POST https://api.resend.com/emails \
            -H "Authorization: Bearer $RESEND_KEY" \
            -H "Content-Type: application/json" \
            -d '{
              "from": "notify@nemik.top",
              "to": "ser4ph4@proton.me",
              "subject": "🚀 WakaTime: Perfil Atualizado!",
              "html": "<h3>✔️ README Atualizado!</h3><p>Suas estatísticas de código foram sincronizadas com sucesso.</p><p>📅 <strong>Data:</strong> '"$NOW"'</p><p>💡 <i>Dica: O tempo total de código pode ser conferido diretamente no seu painel privado.</i></p><p>🔗 <a href=\"https://wakatime.com/dashboard\">Acessar Dashboard do WakaTime</a></p>"
            }'
